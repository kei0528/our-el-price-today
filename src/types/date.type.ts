export type TimeStamp = `${number}-${number}-${number}T${number}:${number}:${number}`;

export type TimeStampDay = `${number}-${number}-${number}`;

export type TimeStampHour = `${number}: ${number}`;
